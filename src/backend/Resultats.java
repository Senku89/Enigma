package backend;

public class Resultats {

    // L'idée est d'avoir cette classe pour stocker tous les résultats (le Score)

    // Attribut ListeScore<Score>
    // L'idée c'est d'envoyer le résultat sur l'ArrayList pour l'afficher à la fin

    // Avoir des setters et getters pour envoyer la liste des résultats vers l'interface
}
